import { AppPage } from '../../types/inertia';
import { SettingsLayout } from '../../layouts/settings/SetingsLayout';

const AccountSettings: AppPage = () => {
  return <SettingsLayout></SettingsLayout>;
};

export default AccountSettings;
